# Copy this file to .env and fill in your own keys
# Then run:  source .env

export ANTHROPIC_API_KEY=""
# Or use DeepSeek through its Anthropic-compatible API:
# export DEEPSEEK_API_KEY=""
# export LLM_PROVIDER="deepseek"
# export DEEPSEEK_MODEL="deepseek-v4-flash"
# Required for Instagram / YouTube and optional non-TTS TikTok analysis.
# TikTok Shop discovery never uses this key; it uses the FastMoss browser session.
export TIKHUB_API_KEY=""
